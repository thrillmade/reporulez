← back to [docs/timeline.md](../timeline.md)

## 2026-06-29 18:08 - re-pin: float the max-mode hook to @next (auto-update; last manual re-pin)

**Reasoning:** rc.20 published; switching the commit-review hook from an exact version pin to floating @next via init --local-only, so it auto-fetches the latest recipe (H1-H4 hardening) every commit and never needs re-pinning again

**Implications:**
- Last re-pin for this repo

---

