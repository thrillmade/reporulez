← back to [docs/timeline.md](../timeline.md)

## 2026-08-24 08:31 - reporulez#68: bin/validate-ruleset.sh -- validate a ruleset's required fields before it is ever applied

**Reasoning:** Nothing checked a ruleset's contents before POST/PUT -- only JSON syntax. An invalid ruleset applied org-wide (apply-org.sh) or per-repo (apply.sh) changed branch protection silently. New standalone bin/validate-ruleset.sh checks three field-level rules against any ruleset JSON: integration-id-pin (SPEC section 6.3, clud-bug-review must pin the clud-bug App id 3944857), admin-bypass-required (SPEC section 6.6, bypass_actors must carry a repository-administrator bypass), and bypass-defeats-deletion-restriction (this repo's own rule, not a SPEC citation -- a deletion rule whose bypass_actors grants an always-bypass to both RepositoryRole admin id=5 AND OrganizationAdmin protects nobody who could otherwise delete the branch). apply.sh and apply-org.sh now run it as a hard gate on the final ruleset JSON before any mutating call and die on violation; audit.sh --include-ruleset runs it read-only against every live-fetched ruleset so a post-apply hand-edit is caught too. Control-tested against real production data, not just synthetic fixtures: gh api repos/thrillmade/agent-skills/rulesets/<id> (read-only) shows all three org-level rulesets on that org, including org-staging which targets refs/heads/dev, already carry the bypass-defeats-deletion-restriction shape -- this is the live ruleset behind the 2026-08-14 dev-branch-deletion incident the check exists for.

**Alternatives considered:** Require all of RepositoryRole maintain(4)+admin(5)+OrganizationAdmin for bypass-defeats-deletion-restriction, matching the incident narrative's 'Repository admin, Maintain, Organization admin' -- rejected after pulling the real ruleset: GitHub does not publish a stable actor_id-to-role mapping beyond RepositoryRole admin=5 (corroborated by this repo's own README, cli/cli#13388, and the nskit Python client's RepositoryRole enum, which encodes only admin=5 for exactly this reason), and the live org-staging/org-baseline/org-default-protection rulesets on thrillmade/agent-skills all carry RepositoryRole actor_id=2, not 4 -- a hardcoded id=4 would never have fired on the incident it was written for, Hard-block admin-bypass-required in apply-org.sh only (zero disruption, org-baseline.json already conforms) and leave apply.sh unguarded or warn-only for it, since baseline/clud-bug/public-guard ship bypass_actors:[] by deliberate, documented design (README: 'OFF by default -- no built-in self-mod use case') -- rejected as inconsistent: same defect class, same validator, smaller blast radius is not zero blast radius, and the die message is fully actionable (--bypass-admin already exists), Leave the bypass-vs-intent check out of reporulez entirely since the actual incident ruleset wasn't applied by reporulez -- rejected once gh api confirmed the same defect exists on reporulez's own toolkit's applied rulesets on live repos (agent-skills' own reporulez-default ruleset, id 16434011, also carries OrganizationAdmin+RepositoryRole(5), diverging from the shipped skdd.json template), making it squarely in scope for the same tool that already audits live drift

**Implications:**
- Real behavior change: baseline/clud-bug/public-guard's default apply.sh invocation (no --bypass-admin) now dies with an actionable error instead of applying. skdd is unaffected (bypass_admin defaults on). apply-org.sh's only variant, org-baseline, already conforms -- zero disruption there
- SPEC section 6.6's second clause (the regenerating identity needs its own distinct bypass) is deliberately NOT checked -- requires per-repo knowledge (does a derived-doc regenerator run here) the ruleset JSON alone can't supply. Flagged, not silently dropped: see PR description
- audit.sh --include-ruleset now surfaces real, live drift: agent-skills' three org-level rulesets (org-baseline, org-default-protection, org-staging) and its repo-level reporulez-default ruleset all currently fail bypass-defeats-deletion-restriction on GitHub today -- read-only finding, not remediated by this change

---

## 2026-08-24 08:38 - reporulez adopts the dev-branch convention, and AGENTS.md stops claiming there is no test suite

**Reasoning:** Two lines in AGENTS.md became false in this PR's own diff. 'There is no test suite' was true when written; tests/test-validate-ruleset.sh lands here with 17 fixture-driven cases, so the doc was telling the next contributor to go looking for a gate sitting right in front of them. Separately, reporulez had no dev branch and nothing in AGENTS.md, README.md, CONVENTIONS.md or docs/ mentioned a dev->main promotion -- which reads as a deliberate single-branch design. It is not one. Every other thrillmade repo lands on dev first and the convention is the organisation's, so adopting it here was never a local call.

**Alternatives considered:** Leave reporulez main-only and document that as deliberate. Rejected: the silence is an omission, not a design, and the org rule is not a per-repo choice. Also considered: split these two corrections into their own PR. Rejected: both are made false BY this diff, and docs are kept true in the same pass the work lands.

**Implications:**
- refs/heads/dev now exists at d3720c1 and this PR targets it; future reporulez work branches from dev. The test-suite line is scoped deliberately -- the suite covers bin/validate-ruleset.sh and nothing else, and the installers stay hand-validated end-to-end. No test count or duration is quoted in the doc, because a hand-kept number with no gate behind it reads as true until one quietly isn't.

---

